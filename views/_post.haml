.item.post
  .banner
    .clipper
      .title
        %h2= post.title
    .clipper
      .metadata
        .meta
          %span.date= format_date(post.published_at)
          %span.dot &#9679;
          %a{ :href => "/blog/#{post.stub}" } permalink
        .tags
          - post.tags.each do |tag|
            %span.tag= tag
  .clipper
    .body
      .content
        :maruku
          #{post.body}
